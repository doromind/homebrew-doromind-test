# homebrew-doromind-test

Test tap for pre-production Doro Mind package builds — mirrors [`doromind/homebrew-doromind`](https://github.com/doromind/homebrew-doromind)'s shape. Used by `../desktop-engineering/welcome-to-doro-mind`'s `task publish:upload:test` to validate a build before it reaches the production tap.
